# Python packages installed into the project venv with pip.
#
# ROS2 packages such as rclpy, std_msgs, sensor_msgs, and cv_bridge are not
# PyPI packages. Install them with apt; see ROS_DEPENDENCIES.md.

numpy==1.26.4
opencv-python==4.13.0.92
torch==2.12.1
torchvision==0.27.1
ultralytics==8.4.72
